# Codex運営ガイド

- 公開内容は `index.html`、法定表示は `legal/index.html` を編集する。
- 商品画像は `images/` に置き、同じファイル名で差し替えるとページ側の変更は不要。
- 価格を変更するときは、`index.html` と `legal/index.html` の全表記を同時に更新する。
- LINEリンク、発送時期、返品条件を変更するときは両ページの整合性を確認する。
- 医療上の効果・効能を保証する表現を追加しない。
- 変更後はリンク切れ、画像表示、スマートフォン幅を確認する。
- `main` への反映後、GitHub ActionsがGitHub Pagesへ自動公開する。
