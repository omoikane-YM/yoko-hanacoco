# Codex運営ガイド

- 表示テンプレートは `index.html`、法定表示は `legal/index.html` にあり、公開用HTMLはビルドで `dist/` に生成する。
- 商品画像は `images/` に置き、同じファイル名で差し替えるとページ側の変更は不要。
- 価格を変更するときは、`src/data/site-facts.json` と `SITE_FACTS.md` を同期し、ビルド後の両ページを確認する。
- LINEリンク、発送時期、返品条件を変更するときは `src/data/site-facts.json` と `SITE_FACTS.md` を同期し、両ページの整合性を確認する。
- 医療上の効果・効能を保証する表現を追加しない。
- 変更後はリンク切れ、画像表示、スマートフォン幅を確認する。
- `main` への反映後、GitHub ActionsがGitHub Pagesへ自動公開する。

# yoko🌸hanacoco🌸 Webサイト運営ルール

このWebサイトについて修正・追加・更新・リファクタリング等を行う際は、必ず作業開始時にこの `AGENTS.md` を確認すること。

## 1. 指示の優先順位

このプロジェクトでは、以下の優先順位で判断する。

1. ユーザーから明示された最新の指示
2. `SITE_FACTS.md` に記載された現在の正式情報
3. `AGENTS.md` に記載されたWebサイト運営ルール
4. 現在のサイトの仕様・デザイン・実装
5. AIによる改善提案・一般的なベストプラクティス

ユーザーから明示的な最新の指示がある場合は、その指示を優先する。ただし、ユーザーが変更を指示した項目だけを変更し、指示されていない関連項目まで勝手に変更しない。

例：

- 「価格を9,800円に変更して」→価格のみ変更し、商品説明、送料、発送条件、注文方法などは変更しない。
- 「トップページの見出しを変更して」→指定された見出しのみ変更し、サイト全体の文章やデザインを勝手に変更しない。

ユーザーからの最新指示と `AGENTS.md` が明確に矛盾する場合は、ユーザーの最新指示を優先する。ただし、指示内容が不明確で重要情報に影響する可能性がある場合は、AIの判断で変更せずユーザーへ確認する。

## 2. サイト基本方針

- スマートフォンでの閲覧を最優先する。
- PC表示よりもスマートフォンでの見やすさ・操作性を重視する。
- テーマカラーはアクア・エメラルドグリーン系とする。
- 優しく、清潔感があり、自然で穏やかなデザインを維持する。
- yoko🌸hanacoco🌸 の現在のブランドイメージと世界観を維持する。
- ユーザーから明示的な指示がない限り、サイト全体のデザインを大きく変更しない。
- AIの判断だけでサイトのコンセプトを変更しない。
- 必要以上のリファクタリングをしない。
- 修正依頼と関係ないファイルを極力変更しない。
- 既存の正常な機能を壊さない。

## 3. 商品

現在の商品価格は **8,800円（税込・送料込）**。

- AIの判断だけで価格を変更しない。
- 商品名を勝手に変更しない。
- 商品素材を勝手に変更しない。
- 商品仕様を勝手に変更しない。
- 販売条件を勝手に変更しない。
- 存在しない商品を追加しない。
- 存在しないサービスや特典を追加しない。

ただし、ユーザーから明示的に変更指示があった項目については、最新指示を優先して変更する。

## 4. 注文

- 注文受付にはLINEを利用する。
- 「注文する」ボタンまたはLINEメッセージを送信した時点では注文確定ではない。
- 入金確認時点で注文確定とする。
- LINEへの注文導線を維持する。
- LINEへの問い合わせ導線を維持する。
- LINE URLをAIの判断で変更しない。
- 注文条件をAIの判断で変更しない。

ユーザーからLINE URLや注文条件について明示的な変更指示があった場合のみ、その指定部分を変更する。

## 5. 発送

現在の発送条件は **入金確認後7営業日以内**。

- 発送条件をAIの判断で変更しない。
- 配送方法をAIの判断で変更しない。
- 送料条件をAIの判断で変更しない。

ユーザーから明示的な変更指示があった場合のみ変更する。

## 6. 表現上の禁止事項

以下をAIの判断で追加しない。

- 医学的・医療的効果の断定
- 治療効果の断定
- 病気が改善するという断定
- 治癒を示す表現
- 予防効果を断定する表現
- 根拠のない商品性能
- 根拠のない研究結果
- 存在しない認証
- 存在しない実績
- 存在しないレビュー
- 存在しない利用者の声

商品説明を改善する場合も、元の意味を勝手に拡張しない。

## 7. 重要情報の保護

以下を「保護対象情報」とし、ユーザーから明示的な変更指示がない限り変更しない。

- 商品価格
- 商品名
- 商品素材
- 商品仕様
- 送料
- 振込先
- 連絡先
- LINE URL
- 注文確定条件
- 発送条件
- 配送方法
- 返品条件
- 法的表記
- その他、購入者との取引条件に関わる情報

ユーザーから変更指示があった場合は、指定された項目のみ変更する。関連する保護対象情報まで推測して変更しない。

## 8. デザイン変更時のルール

デザイン変更・CSS変更・レイアウト変更を行う場合は、スマートフォン表示を最優先し、必ず以下を確認する。

- 横スクロールが発生していないこと。
- テキストが画面からはみ出していないこと。
- 画像が画面からはみ出していないこと。
- ボタンが押しやすいこと。
- 重要な情報が見切れていないこと。
- 文字が小さすぎないこと。
- 商品価格が分かりやすいこと。
- LINE注文ボタンが分かりやすいこと。
- LINE問い合わせボタンが分かりやすいこと。
- アクア・エメラルドグリーン系の世界観が維持されていること。
- PC表示にも致命的なレイアウト崩れがないこと。

## 9. 修正範囲の最小化

ユーザーから修正依頼を受けた場合は、原則として必要最小限の変更を行う。

例えば「この文章を変更して」という依頼に対して、別の文章、デザイン、商品情報を変更したり、CSSを全面的に書き換えたり、コンポーネント構成を変更したりしない。リファクタリングについても、依頼された修正に必要でない大規模変更は避ける。

## 10. AIによる自主的な改善

AIが改善点を発見した場合でも、重要な変更を勝手に実施せず、原則として改善案としてユーザーへ提案する。

ただし、以下のような内容・デザイン・取引条件に影響しない軽微な修正は、安全性を確認した上で対応できる。

- 明らかなコードエラーの修正
- ビルドエラーの修正
- 明らかなリンク記述ミス
- アクセシビリティ上の軽微な技術修正
- コードフォーマットの整理
- 明らかな不要デバッグコードの削除

重要情報や表示内容に影響する可能性がある場合は、ユーザーへ確認する。

## 11. 修正後の必須確認

コードを変更した場合は、作業完了前に可能な範囲で以下を確認する。

1. ビルドエラーがない。
2. 実行時エラーがない。
3. 内部リンクが正常。
4. 外部リンクが正常。
5. LINEリンクが正常。
6. 画像が正常に表示される。
7. スマートフォン幅でレイアウト崩れがない。
8. 横スクロールが発生していない。
9. PC表示で致命的なレイアウト崩れがない。
10. 商品価格が意図せず変更されていない。
11. 商品情報が意図せず変更されていない。
12. 注文条件が意図せず変更されていない。
13. 発送条件が意図せず変更されていない。
14. LINE URLが意図せず変更されていない。
15. 不要なテストコード・デバッグコードを残していない。

## 12. 作業前の確認

修正を開始する前に以下を確認する。

- `AGENTS.md`
- `SITE_FACTS.md`
- 現在のサイト構造
- 修正対象ファイル
- ユーザーからの最新指示

現在正常に動いている機能を壊さないことを優先する。

## 13. 作業完了時の報告

修正後はユーザーへ簡潔に以下を報告する。

- 変更した内容
- 変更したファイル
- 実施した確認・テスト
- 問題があればその内容

変更していない重要情報について、不要に長い報告はしない。

## 14. 不明点がある場合

ユーザーの指示から複数の解釈ができ、それによって商品情報・価格・注文・発送・LINE・法的表記など重要情報が変わる場合は、推測で変更せず、必要に応じてユーザーへ確認する。

一方、明確な指示については不要な確認質問を行わず、そのまま作業する。

## 15. 必須の作業フロー

今後CodexがWebサイトを修正するときは、必ず以下の順番で作業する。

1. ユーザーの最新指示を読む。
2. `AGENTS.md` を確認する。
3. `SITE_FACTS.md` を確認する。
4. 修正対象ファイルを確認する。
5. 必要最小限の変更を実施する。
6. `SITE_FACTS.md` と矛盾がないか確認する。
7. `CHECKLIST.md` に沿って確認する。
8. ビルドテストを行う。
9. 必要な場合のみ `SITE_FACTS.md` を更新する。
10. Webサイト本体を変更した場合は `CHANGELOG.md` に変更内容を追記する。運営・管理ファイルのみの変更は、必要な場合に限り記録する。
11. ユーザーへ結果を報告する。

## 16. SITE_FACTS.md の同期

- 正式な構造化データは `src/data/site-facts.json` とする。
- 人間向けの確認用正式情報は `SITE_FACTS.md` とする。
- `src/data/site-facts.json` と `SITE_FACTS.md` が矛盾する場合は、自動修正せず差異として報告する。
- ユーザーの最新の明示的な指示を最優先し、指示された項目だけを変更する。
- 重要情報をHTMLやコンポーネントへ新たに直接ベタ書きしない。
- 同じ正式情報を複数の実装箇所に重複保存せず、可能な限り `src/data/site-facts.json` を参照して表示する。
- ユーザーの正式な指示により重要情報を変更する場合は、`src/data/site-facts.json`、`SITE_FACTS.md`、必要なサイト表示を同じ作業内で同期する。
- 同期対象はユーザーが指示した項目だけとし、関連情報を推測で変更しない。
- Webサイト本体を変更した場合は `CHANGELOG.md` に日付、変更内容、対象ファイル、理由を記録する。

## 17. GitHub安全運用

- `main` ブランチを公開用とする。
- 大きな変更は、可能な場合は別ブランチで行う。
- ビルドまたは必須確認が失敗した状態を公開しない。
- Secrets、個人情報、APIキー、パスワード、振込先などをコードへ直接書かず、GitHubへコミットしない。
- `.env` など秘密情報を含み得るファイルをGit管理対象外にする。
- GitHub Actionsには必要最小限の権限のみ与える。
- 既存のGitHub Actions設定を変更する場合は、現在の公開フローを壊さないことを確認する。
- 可能な場合は、依存関係の導入、ビルド、HTML・リンクの基本確認を公開前に自動実行し、成功時のみデプロイする構成を提案する。

## 18. 最小変更原則で避ける事項

依頼に必要がない限り、以下を行わない。

- CSSの全面変更
- コンポーネントの全面再構成
- 使用技術の変更
- デザイン刷新
- 不要なファイル移動またはファイル名変更
- 依存パッケージの大量追加
